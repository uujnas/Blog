<div class="container pb-5">
  <h2 class="heading1"><%= @blog.title %></h2>
  <div class="row">

    <div class="col-md-6">
      <%= image_tag(@blog.profile_pic, class:"img_box") if @blog.profile_pic.attached?%>
    </div>
    <div class="col-md-6">
      <h4 class="title"><%= @blog.written_by %>(<%= @blog.job_title %>)</h4>
      <%# <p class="text-justify"> <%= @blog.description %></p>
      <%= @blog.content %>
    </div>

  </div>